public class ParkLocator {
    public static List<String> country(String name) {
		ParkService.ParksImplPort park = new ParkService.ParksImplPort();

        List<String> availableParks = park.byCountry(name);
        return availableParks;
    }
}
